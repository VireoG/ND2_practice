@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Admin Page"];
}

@if (User.Identity.IsAuthenticated)
{
<h1>Admin page</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <h3>Admin functions</h3>
        <form asp-action="Index">
            <div class="nav-item">
                <div class="btn-group-vertical">                                    
                        @Html.ActionLink("Role Manager", "Index", "Roles", new { }, new { @class = "btn btn-primary btnsg" })
                        @Html.ActionLink("Add New City", "CreateCity", "Admin", new { }, new { @class = "btn btn-primary btnsg" })
                        @Html.ActionLink("Add New Venue", "CreateVenue", "Admin", new { }, new { @class = "btn btn-primary btnsg" })
                        @Html.ActionLink("Create New Event", "CreateEvent", "Admin", new { }, new { @class = "btn btn-primary btnsg" })
                </div>
            </div>
        </form>
    </div>
</div>
}

